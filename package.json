{
  "name": "node-lazyloader",
  "version": "1.0.1",
  "description": "A dynamic file loader for node, loads files if they are in a directory",
  "main": "index.js",
  "scripts": {
    "test": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AgencyPMG/node-lazyloader.git"
  },
  "keywords": [
    "dynamic",
    "loading"
  ],
  "author": "Chris Alvares <mail@chrisalvares.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/AgencyPMG/node-lazyloader/issues"
  },
  "homepage": "https://github.com/AgencyPMG/node-lazyloader",
  "dependencies": {
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "coveralls": "^2.11.2",
    "istanbul": "^0.3.13",
    "mocha": "^2.2.4",
    "mocha-lcov-reporter": "0.0.2"
  }
}
